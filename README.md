# Starting the app

## step 1:

Create the poflow db in mysql

## step 2:

Run the following command after cloning the repo.

`npm run total && npm run seeds`

## step 3:

after that finishes installing, run:

`npm run start:dev`

## DON'T FORGET

grab the .env file from the dcschools slack channel
